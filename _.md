#84 Выполнить чек-лист ручного тестирования
- Text
  - [x] Forward.SendCopy (or forward)
  - [x] and edit sync
  - [?] and edit sync for double copy
  - [x] Forward.CopyOnce (edit sync)
  - [x] Forward.Indelible (delete sync)
  - [x] Forward.Exclude
  - [x] Forward.Include
  - [x] Forward.IncludeSubmatch
  - [ ] ReplaceMyselfLinks + DeleteExternal
  - [ ] ReplaceFragments (and not equal len)
  - [x] Sources.Link + Title
  - [x] Sources.Sign
  - [ ] AutoAnswers
- MediaAlbum
  - [ ] Forward.SendCopy (or forward)
  - [ ] Forward.CopyOnce (edit sync)
  - [ ] Forward.Indelible (delete sync)

TODO
- [x] неверная дата util.GetCurrentDate()
- [x] переопределить engine.e2e.yml
- [x] собрать UniqieDistonations по ForwardRules.To
- [x] перед стартом и при перезагрузке конфига выполнять в цикле LoadChats с проверкой GetChatHistory(local), пока не получим все UniqieDistonations (но если удалён - то мы получим бесконечный цикл, а потому ограничение рекурсии на три попытки)
- [x] pplog
- [ ] веб-транспорт с ручками создания чатов, записи и чтения сообщений для автоматизации e2e-тестов
- [ ] составить подробный план e2e-тестирования
- [ ] реализовать e2e-тесты

- [x] проверить весь код, что `defer s.log.ErrorOrDebug` только там, где нет возврат ошибки
- [x] добавить `log.WrapError()` в методы `repo/telegram/client_adapter.go`
- [x] добавить `log.WrapError(err) // внешняя ошибка`
- [x] создавать ли ошибки на отсутствие входных данных? - да, зафиксировал в соглашении
- [x] `assert.Equal(t, slog.LevelError, records[0].Level)` - убрать, т.к. перевёл LogLevel на error
- [x] перевернуть callstack

- [ ] исследовать механизм tmpChatId
- [ ] исследовать AutoAnswer
- [x] зочем forward-rules[].other & forward-rules[].check
- [x] как добавляется пачка сообщений без тика раз в секунду
- [ ] добавить тесты при сборке для разных платформ

- [ ] Выбрать BDD для e2e тестов
https://github.com/onsi/ginkgo
https://github.com/hedhyw/gherkingen
https://github.com/smartystreets/goconvey/
https://github.com/onsi/gomega
